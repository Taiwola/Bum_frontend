import { Lane } from '@/types/types'
import React from 'react'

type Props = {
    pipelineId: string,
    defaultData?: Lane
}

export default function CreateLaneForm({}: Props) {

  return (
    <div>createLane</div>
  )
}